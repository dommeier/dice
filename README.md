# Mixed Diffusion: A Unified Framework for Denoising Single-Cell Data

A PyTorch implementation of mixed diffusion models for single-cell genomics data denoising and analysis. This framework combines diffusion models with Gibbs sampling to denoise high-dimensional biological data, particularly focused on single-cell RNA sequencing (scRNA-seq) and CITE-seq data.

## Overview

Mixed Diffusion addresses the challenge of noise in single-cell genomics data by learning the underlying data distribution through diffusion processes. The model can handle both synthetic and real biological datasets, providing robust denoising capabilities for downstream analysis tasks like clustering and trajectory inference.

## Installation

### Prerequisites

- Python 3.8+
- PyTorch 1.9+
- CUDA-compatible GPU (recommended)

### Setup

1. Clone the repository:
```bash
git clone <repository-url>
cd mixed_diffusion
```

2. Install the package:
```bash
pip install -e .
```

3. Install additional preprocessing dependencies:
```bash
pip install -r requirements-preprocessing.txt
```

## Quick Start

### Basic Usage

The `scripts/main.py` combines new training of the diffusion model using the option `--from_scratch`, and running the gibbs sampling procedure:
```bash
python scripts/main.py \
            --model_dir MODEL_DIRECTORY \
            --gibbs_iterations 200 \
            --rho_scheduling_type linear \
            --rho_start 1.0 \
            --rho_end .5 \
            --initial_x measurement \
            --result_dir RESULTS_DIRECTORY \
            --save_data \
            --initial_x measurement \
            --test_on test \
            --from_scratch \
            --checkpoints_to_keep 2000
```
To re-use a trained diffusion model, just omit `--from_scratch`, which will load the model checkpoint from `MODEL_DIRECTORY` and immediately run Gibbs sampling.

## Model Architectures

### TabularDiffusionMLP
Multi-layer perceptron designed for tabular single-cell data with time embedding and residual connections.

## Advanced Usage

### Grid Search Optimization

Run hyperparameter optimization:
```bash
python scripts/grid_search_synthetic.py --output_dir results/grid_search
```

### Clustering Analysis

The clustering analysis of embeddings is performed by a custom R script:
```bash
Rscript scripts/clustering_metrics.R {results_folder}
```


# Paper results

## Section 4: Synthetic experiments
- Data generation: `data/synthetic_d/generation.ipynb`
- Results for DICE can be obtained by running `scripts/grid_search_synthetic_d.py` with the appropriate hyperparameters.
- Graphics are generated by `notebooks/04_synthetic_experiments.ipynb`


## Real-world experiments
- Data: Data for real world experiments is in
`data/CITEseq` and `data/neo_cortex`, along with preprocessing scripts to save them as `.npy` files which are loaded by `DICE`.
### Neo_Cortex:

To obtain results for Neo_Cortex, run
```
python scripts/main.py \
            --model_dir models/neo_cortex/k_ablation/15d \
            --gibbs_iterations 200 \
            --rho_scheduling_type linear \
            --rho_start 5.0 \
            --rho_end 0.5 \
            --initial_x measurement \
            --result_dir results/neo_cortex/k_ablation/15d \
            --save_data \
            --initial_x measurement \
            --test_on test \
            --from_scratch \
            --checkpoints_to_keep 2000
```

### CITEseq
To obtain results for CITEseq, run
```
python scripts/main.py \
            --model_dir models/CITEseq/k_ablation/25d \
            --gibbs_iterations 200 \
            --rho_scheduling_type linear \
            --rho_start 5.0 \
            --rho_end 0.5 \
            --initial_x measurement \
            --result_dir results/CITEseq/k_ablation/25d \
            --save_data \
            --initial_x measurement \
            --test_on test \
            --from_scratch \
            --checkpoints_to_keep 2000
```

Plots and clustering metrics can then be generated by `Rscript scripts/clustering_metrics.R {results_folder}`

### Benchmarking

Benchmarking results can be found in `benchmarks/{dataset}`. All benchmarks are executed in `benchmarks/{dataset}/benchmarks.ipynb`, except
scVI due to long running time, which was run using `scripts/run_scvi_benchmark.py`.
`benchmarks/gather_tables.ipynb` collects all benchmarking data and outputs the Latex tables.

### Appendix E:
- Data generation: `data/synthetic_d` and `data_archetypes`.
- Plots: Plots are generated with the notebooks `appendix*.ipynb`.

# Citation

If you use this code in your research, please cite:
```bibtex
@misc{meier2026clusteringdenoisinglatentplugandplay,
      title={Clustering by Denoising: Latent plug-and-play diffusion for single-cell data}, 
      author={Dominik Meier and Shixing Yu and Sagnik Nandy and Promit Ghosal and Kyra Gan},
      year={2026},
      eprint={2510.22835},
      archivePrefix={arXiv},
      primaryClass={cs.LG},
      url={https://arxiv.org/abs/2510.22835}, 
}
```